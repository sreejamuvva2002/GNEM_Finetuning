# Generated weights stay local; metadata and evidence remain tracked. See docs/REPOSITORY_MAINTENANCE.md.
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Repo-local Phase 6+ runtime environment. Never committed; the v2
# repository's .venv-ft is deliberately NOT used (CLAUDE.md 7).
.venv-v3/

# Isolated pinned model-training runtime.
.venv-v3-train/

# Large artifacts: preserve locally, never add to ordinary Git.
*.safetensors
*.pt
*.pth
/review_v2_questions/all_predictions.jsonl

# Private update-recovery copies: never publish chat history or local state.
/.recovery/

# Recovery upload contains private chat history and archived model artifacts.
/finetune/lab_update_2026-09-13/

/lab_update_2026-09-13/

/excluded_artifacts.tar

/codex_history.tar.gz
